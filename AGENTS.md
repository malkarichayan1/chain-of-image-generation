# Agent instructions

See **[CLAUDE.md](CLAUDE.md)** for the project's working context: the paper's thesis and arc,
what each result actually supports, the repository map, and what to do next.

This file is deliberately a pointer rather than a copy. It used to be a hand-copied duplicate of
CLAUDE.md and drifted out of date, which is exactly the failure mode the 2026-08-06 consolidation
removed. Keep it a pointer.
